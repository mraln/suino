<%= form_for @matriz, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :numid, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :numid, :class => 'text_field' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :nome, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :nome, :class => 'text_field' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :raca, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :raca, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                matrizes_path, :class => 'btn' %>
  </div>
<% end %>
